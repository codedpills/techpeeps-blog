# Pipeline dependencies (Python 3.10+)
requests>=2.31
python-dotenv>=1.0
yt-dlp>=2024.1.0
assemblyai>=0.30
anthropic>=0.40
PyYAML>=6.0
# ffmpeg and the `gh` CLI must be installed separately (system packages).
